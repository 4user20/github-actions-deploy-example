# This Dockerfile is a reference point.
# The actual app build uses example-app/apps/api/Dockerfile
# See ../docker-nginx-letsencrypt-template/example-app/ for the full app

FROM alpine:3.19
RUN echo "This is a CI/CD demo placeholder. Replace with actual app build." > /message.txt
CMD ["cat", "/message.txt"]
